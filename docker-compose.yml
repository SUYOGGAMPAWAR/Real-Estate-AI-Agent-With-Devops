version: '3.8'

services:
  ai-automation-engine:
    build: .
    container_name: real_estate_ai_backend
    ports:
      - "8000:8000"
    environment:
      - OPENAI_API_KEY=your_placeholder_key
      - WHATSAPP_TOKEN=your_placeholder_token
    restart: always